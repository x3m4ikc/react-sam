import { Box } from "@mui/material"

const TopBar = () => {
    return (
        <Box sx={{display: "flex", justifyContent: "space-between", px}}>

        </Box>
    )
}

export default TopBar;

// #17 10 minutes